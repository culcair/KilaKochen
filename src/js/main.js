import { Dropdown, Collapse, Alert } from 'bootstrap';

// This file bundles only the required Bootstrap components.
// Popper.js is automatically handled by esbuild if imported by Bootstrap components.

export default {
    Dropdown,
    Collapse,
    Alert
};
